# @soorria/logger

## 2.0.0

### Major Changes

- 000000c: Initial 1.0 release
  - Structured JSON logging with `JsonLogFormatter`
  - Pretty colored output for development with `PrettyLogFormatter`
  - Async context tracking with `runWithContext` and `getLogContext`
  - Child loggers with scoped metadata
  - Configurable log levels
  - Console transport

## 1.0.0

### Major Changes

- Initial 1.0 release
  - Structured JSON logging with `JsonLogFormatter`
  - Pretty colored output for development with `PrettyLogFormatter`
  - Async context tracking with `runWithContext` and `getLogContext`
  - Child loggers with scoped metadata
  - Configurable log levels
  - Console transport
